package day26;

public class Rules
{

}


//1. abstract methods cannot be final --as final method cannot be overridden
//2. abstract methods cannot be static
//3. abstract method cannot have access modifier as private
//4. Variables in an interface will always be static and final by default
//5. Static method from class can be inherited into child class but static method from parent interface cannot be inherited in child class.
//6. Default method cannot be called using Interface name as its not static and we cannot even make default method as static

